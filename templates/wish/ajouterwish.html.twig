{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(wishForm) }}
        {{ form_widget(wishForm) }}
        <button>Ajouter</button>
        {{ form_end(wishForm) }}
    </div>
{% endblock %}

